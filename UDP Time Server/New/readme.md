## UDP Time Server Program(Modified like previous UDP Program)

To run:Create two terminal windows and type following commands

Server:

    gcc udps.c -o s
    ./s
 
Client:

    gcc udpc.c -o c
    ./c
